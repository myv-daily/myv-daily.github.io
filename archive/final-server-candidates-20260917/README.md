# 日报研究与名单历史快照

2026-09-17，Asia/Shanghai。保存旧服务器独有的研究候选与有代表性的名单结果。`manifest.json` 记录原路径、SHA-256、历史状态与对象路径；原字节在 `objects/`。不修改网站当前内容，不把草稿标记为正式完成，不恢复自动发布。

重复的原始行情缓存和旧队列不全量保存；代表性最新排名保留其历史时点含义，不能当作当前排名使用。
